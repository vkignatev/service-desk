#docker-compose up
version: "3.9"
services:
  postgres:
    container_name: localPostgresSQLDBSD
    image: postgres
    environment:
      POSTGRES_DB: "local_db_sd"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "12345"
    ports:
      - "5434:5432"
